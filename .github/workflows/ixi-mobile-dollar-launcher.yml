name: IXI Mobile AOS Dollar Launcher Certification

on:
  push:
    branches:
      - mobile/commercial-aos-v1-dollar-launcher
    paths:
      - "pages/mobile-aos-foundation.js"
      - "lib/ixi-mobile/**"
      - "tests/ixi-mobile-dollar-launcher-contract.test.mjs"
      - ".github/workflows/ixi-mobile-dollar-launcher.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ixi-mobile-dollar-launcher-${{ github.ref }}
  cancel-in-progress: false

jobs:
  certify:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      NEXT_PUBLIC_SHARETRIBE_CLIENT_ID: ci-placeholder

    steps:
      - name: Checkout exact branch commit
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install repository dependencies
        run: npm install --no-audit --no-fund

      - name: Run mobile dollar launcher contract
        run: node --test tests/ixi-mobile-dollar-launcher-contract.test.mjs

      - name: Build production application
        run: npm run build
