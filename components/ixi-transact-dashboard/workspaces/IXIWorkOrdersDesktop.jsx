const money=(value,currency="USD")=>Number.isFinite(Number(value))?new Intl.NumberFormat("en-US",{style:"currency",currency,maximumFractionDigits:0}).format(Number(value)):"—";
function Metric({label,value,tone=""}){return <div className={`tdw-metric ${tone}`}><span>{label}</span><strong>{value}</strong></div>}
const arr=v=>Array.isArray(v)?v:[];
export default function IXIWorkOrdersDesktop({model={},onOpenRecord=()=>{}}){const c=model.currency||"USD",rows=model.workOrders||[];return <section className="tdw"><div className="td-workspace-title"><div><span>IXI TRAN$ACT · OPERATIONS</span><h1>WORK ORDERS</h1></div><div className="td-workspace-meta"><span>CANONICAL WORK</span><strong>TIME · MATERIAL · SERVICE · EXPENSE · PURCHASE · DOCUMENT · NOTE · PHOTO</strong></div></div><div className="tdw-metrics"><Metric label="OPEN" value={model.summary?.open??"—"}/><Metric label="IN PROGRESS" value={model.summary?.inProgress??"—"}/><Metric label="WAITING" value={model.summary?.waiting??"—"}/><Metric label="OVERDUE" value={model.summary?.overdue??"—"} tone={Number(model.summary?.overdue)>0?"bad":""}/><Metric label="COST THIS MONTH" value={money(model.summary?.monthCost,c)}/></div><div className="tdw-panel"><div className="tdw-head"><span>OPERATING QUEUE</span><strong>COMPANY WORK ORDERS</strong></div><div className="tdw-table-head wo"><span>WO</span><span>ASSET</span><span>LOCATION</span><span>WORK</span><span>OWNER</span><span>COST</span><span>STATUS</span></div><div className="tdw-table">{rows.length?rows.map((row,i)=><button className="tdw-table-row wo" key={row.workOrderId||row.number||i} onClick={()=>onOpenRecord({...row,title:row.number||row.workOrderId||"WORK ORDER",sourceRecordId:row.workOrderId||row.number,sourceRecordType:"work-order",workspace:"operations",detail:row.title||"Canonical Work Order"})}><span>{row.number||row.workOrderId||"—"}</span><span>{row.assetLabel||row.assetPassportId||"—"}</span><span>{row.locationLabel||"—"}</span><span>{row.title||"—"}</span><span>{row.assignedLabel||"—"}</span><span>{money(row.totalActual,c)}</span><b className={["overdue","blocked"].includes(String(row.status).toLowerCase())?"bad":""}>{row.status||"OPEN"}</b></button>):<div className="td-empty-state compact">NO WORK ORDERS RETURNED BY SERVER PROJECTION</div>}</div></div>{rows.length?<div className="tdw-grid"><div className="tdw-panel"><div className="tdw-head"><span>COST CONTROL</span><strong>TOP WORK ORDER COST</strong></div><div className="tdw-list">{rows.slice().sort((a,b)=>(Number(b.totalActual)||0)-(Number(a.totalActual)||0)).slice(0,10).map((row,i)=><button key={row.workOrderId||i} onClick={()=>onOpenRecord({...row,title:row.number||row.workOrderId,sourceRecordId:row.workOrderId||row.number,sourceRecordType:"work-order",workspace:"operations"})}><div><strong>{row.number||row.workOrderId} · {row.assetLabel||"ASSET"}</strong><small>LABOR {money(row.laborActual,c)} · MATERIAL {money(row.materialActual,c)} · SERVICE {money(row.serviceActual,c)}</small></div><b>{money(row.totalActual,c)}</b></button>)}</div></div><div className="tdw-panel"><div className="tdw-head"><span>RELATIONSHIP CONTROL</span><strong>WORK RECORD COVERAGE</strong></div><div className="tdw-list">{rows.slice(0,10).map((row,i)=>{const r=row.references||{};const count=arr(r.timeEntryIds).length+arr(r.materialRecordIds).length+arr(r.serviceRecordIds).length+arr(r.expenseIds).length+arr(r.purchaseOrderIds).length+arr(r.billIds).length+arr(r.photoIds).length+arr(r.noteIds).length;return <button key={row.workOrderId||i} onClick={()=>onOpenRecord({...row,title:row.number||row.workOrderId,sourceRecordId:row.workOrderId||row.number,sourceRecordType:"work-order",workspace:"operations"})}><div><strong>{row.number||row.workOrderId}</strong><small>{count} RELATED WORK / FINANCIAL RECORDS</small></div><b>{row.status||"OPEN"}</b></button>})}</div></div></div>:null}</section>}
