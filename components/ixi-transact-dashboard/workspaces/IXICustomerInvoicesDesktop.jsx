import IXIEnterpriseDataTable from "../components/IXIEnterpriseDataTable";
const clean=v=>String(v??"").trim();
const money=(v,c="USD")=>v===null||v===undefined?"—":new Intl.NumberFormat("en-US",{style:"currency",currency:c,maximumFractionDigits:2}).format(Number(v));
const date=v=>{const s=clean(v);if(!s)return"—";const d=new Date(s);return Number.isNaN(d.getTime())?s:d.toLocaleDateString("en-US")};
export default function IXICustomerInvoicesDesktop({model={},onOpenRecord}){const rows=Array.isArray(model.receivables)?model.receivables:[],columns=[{key:"invoiceNumber",label:"INVOICE",width:"minmax(130px,.8fr)",sortable:true},{key:"customerLabel",label:"CUSTOMER",width:"minmax(190px,1.3fr)",sortable:true},{key:"invoiceType",label:"TYPE",width:"120px",sortable:true,value:r=>clean(r.invoiceType).replace(/-/g," ").toUpperCase()},{key:"invoiceDate",label:"INVOICE DATE",width:"120px",sortable:true,render:r=>date(r.invoiceDate)},{key:"dueDate",label:"DUE",width:"120px",sortable:true,render:r=>date(r.dueDate)},{key:"originalAmount",label:"ORIGINAL",width:"120px",sortable:true,render:r=><b>{money(r.originalAmount,r.currency||model.currency)}</b>},{key:"received",label:"RECEIVED",width:"120px",sortable:true,render:r=>money(r.received,r.currency||model.currency)},{key:"credited",label:"CREDITS",width:"110px",sortable:true,render:r=>money(r.credited,r.currency||model.currency)},{key:"balance",label:"OPEN",width:"120px",sortable:true,render:r=><b>{money(r.balance,r.currency||model.currency)}</b>},{key:"status",label:"STATUS",width:"100px",sortable:true,value:r=>clean(r.status).toUpperCase()}];return <section className="td-workspace"><div className="td-workspace-title"><div><span>MONEY IN · REVENUE DOCUMENTS</span><h1>CUSTOMER INVOICES</h1><p>Canonical issued invoices, receipts, credits and open balance. Collections remains the operating follow-up surface.</p></div></div><div className="td-kpi-strip"><div className="td-kpi"><span>OPEN A/R</span><strong>{money(model.totals?.totalAR,model.currency)}</strong></div><div className="td-kpi"><span>CURRENT</span><strong>{money(model.totals?.current,model.currency)}</strong></div><div className="td-kpi"><span>OVERDUE</span><strong>{money(model.totals?.overdue,model.currency)}</strong></div><div className="td-kpi"><span>INVOICES RETURNED</span><strong>{rows.length}</strong></div></div><IXIEnterpriseDataTable ariaLabel="IXI customer invoices" columns={columns} rows={rows} rowKey={r=>r.invoiceId||r.invoiceNumber} onRowOpen={r=>onOpenRecord?.({...r,title:r.invoiceNumber||r.invoiceId,sourceRecordType:"invoice",sourceRecordId:r.invoiceId,recordType:"invoice",recordId:r.invoiceId,financialDocumentId:r.invoiceId,passportId:r.customerPassportId,workspace:"invoices",detail:`${r.customerLabel||"CUSTOMER"} · ${money(r.balance,r.currency||model.currency)} open`})} emptyLabel="NO CUSTOMER INVOICES RETURNED FOR THIS SCOPE." /></section>}
