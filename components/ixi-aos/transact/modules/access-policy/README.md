# ACCESS / POLICY

Universal TRAN$ACT administration surface for IXI Authority.

The module operates on the currently selected AOS object. It does not infer whether an object is an employee or any other business class from its name. An authorized administrator can explicitly grant login access to the selected object and can manage object policy. Any Passport-bearing object may carry policy, including containers whose policy can propagate to descendants.
