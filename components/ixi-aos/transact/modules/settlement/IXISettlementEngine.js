const clean=v=>String(v??"").trim();const num=v=>Number.isFinite(Number(v))?Number(v):0;const money=v=>Math.round(num(v)*100)/100;const arr=v=>Array.isArray(v)?v:[];
function amount(r={}){return money(r.amount??r.financial?.amount??r.bill?.amount??r.expense?.amount??r.payment?.amount??r.economics?.amount??0)}function type(r={}){return clean(r.transactModule||r.metadata?.transactModule||r.documentType||r.type).toLowerCase()}
export function projectIXIAssetSettlement({sale={},acquisition={},financialRecords=[],manual={}}={}){const records=arr(financialRecords),salePrice=money(sale.sale?.salePrice),collected=money(sale.collection?.amountReceived),acqDirect=money(acquisition.acquisition?.directAcquisitionCost),makeReady=money(acquisition.makeReady?.actualTotal||acquisition.acquisition?.estimatedMakeReady),postCosts=records.filter(r=>["expense","bill","work-order","time-entry","material-usage","technology-work-order"].includes(type(r))&&!r.metadata?.acquisitionCost&&!r.acquisitionCost).reduce((s,r)=>s+amount(r),0),assetIncome=records.filter(r=>["rental-income","service-invoice","invoice"].includes(type(r))&&r.metadata?.assetIncome!==false&&!r.metadata?.assetSale).reduce((s,r)=>s+amount(r),0),sellingCosts=money(manual.sellingCosts??records.filter(r=>r.metadata?.settlementCost||r.metadata?.sellingCost||r.settlementCost).reduce((s,r)=>s+amount(r),0)),liens=money(manual.lienPayoffs??arr(manual.liabilities).filter(x=>x.status!=="paid").reduce((s,x)=>s+num(x.amount),0)),reimbursements=money(arr(manual.reimbursements).filter(x=>x.status!=="paid").reduce((s,x)=>s+num(x.amount),0)),priorDistributions=money(arr(manual.priorDistributions).reduce((s,x)=>s+num(x.amount),0)),grossInvested=money(acqDirect+makeReady+postCosts),netEconomicInvestment=money(grossInvested-assetIncome),netSaleValue=money(salePrice-sellingCosts),economicProfit=money(netSaleValue-netEconomicInvestment),cashAvailableBeforeOwners=money(Math.max(0,collected-sellingCosts-liens)),buyerBalance=money(Math.max(0,salePrice-collected));return{salePrice,collected,buyerBalance,acquisitionCost:acqDirect,makeReadyCost:makeReady,postAcquisitionCosts:money(postCosts),assetIncome:money(assetIncome),grossInvestedCost:grossInvested,netEconomicInvestment,sellingCosts,lienPayoffs:liens,outstandingReimbursements:reimbursements,priorDistributions,netSaleValue,economicProfit,cashAvailableBeforeOwners};}
export function calculateIXISettlementWaterfall({owners=[],projection={},capitalEvents=[],reimbursements=[],priorDistributions=[],returnCapitalFirst=true}={}){const normalized=arr(owners).map((o,i)=>({ownerId:clean(o.ownerId)||`OWNER-${i+1}`,partyPassportId:clean(o.partyPassportId),partyId:clean(o.partyId),label:clean(o.partyLabel||o.label),settlementSharePercent:num(o.settlementSharePercent??o.legalOwnershipPercent),legalOwnershipPercent:num(o.legalOwnershipPercent),openingCapital:money(o.initialContribution)}));const capitalBy=new Map(normalized.map(o=>[o.ownerId,o.openingCapital]));for(const e of arr(capitalEvents)){const id=clean(e.ownerId),delta=num(e.amount)*(clean(e.type).includes("distribution")||clean(e.type).includes("reimbursement")?-1:1);capitalBy.set(id,money(num(capitalBy.get(id))+delta));}const reimbBy=new Map(),priorBy=new Map();for(const x of arr(reimbursements))reimbBy.set(clean(x.ownerId),money(num(reimbBy.get(clean(x.ownerId)))+num(x.amount)));for(const x of arr(priorDistributions))priorBy.set(clean(x.ownerId),money(num(priorBy.get(clean(x.ownerId)))+num(x.amount)));const shareTotal=normalized.reduce((s,o)=>s+o.settlementSharePercent,0),capitalTotal=normalized.reduce((s,o)=>s+Math.max(0,num(capitalBy.get(o.ownerId))),0),reimbTotal=Array.from(reimbBy.values()).reduce((s,x)=>s+x,0),available=money(projection.cashAvailableBeforeOwners),afterReimb=money(Math.max(0,available-reimbTotal)),capitalPool=returnCapitalFirst?money(Math.min(afterReimb,capitalTotal)):0,residual=money(Math.max(0,afterReimb-capitalPool));const rows=normalized.map(o=>{const capitalOutstanding=Math.max(0,num(capitalBy.get(o.ownerId))),capitalReturn=capitalTotal?money(capitalPool*(capitalOutstanding/capitalTotal)):0,profitShare=shareTotal?money(residual*(o.settlementSharePercent/shareTotal)):0,reimbursement=money(reimbBy.get(o.ownerId)),prior=money(priorBy.get(o.ownerId)),grossEntitlement=money(reimbursement+capitalReturn+profitShare),finalDue=money(Math.max(0,grossEntitlement-prior));return{...o,capitalOutstanding,reimbursement,capitalReturn,profitShare,priorDistributions:prior,grossEntitlement,finalDue,paid:0,balanceDue:finalDue};}),totalFinalDue=money(rows.reduce((s,x)=>s+x.finalDue,0)),unallocatedCash=money(Math.max(0,available-totalFinalDue));return{shareTotal:money(shareTotal),capitalTotal:money(capitalTotal),reimbursementTotal:money(reimbTotal),availableCash:available,capitalReturnPool:capitalPool,residualProfitPool:residual,owners:rows,totalFinalDue,unallocatedCash,balanced:Math.abs((totalFinalDue+unallocatedCash)-available)<.01};}
export function getIXISettlementBlockers({sale={},projection={},waterfall={},liabilities=[]}={}){const blockers=[];if(!sale?.identity?.saleId&&!sale?.identity?.number)blockers.push("SALE RECORD REQUIRED");if(num(projection.buyerBalance)>.005)blockers.push("BUYER BALANCE OUTSTANDING");if(arr(liabilities).some(x=>x.status!=="paid"&&num(x.amount)>0))blockers.push("LIEN / PAYOFF OPEN");if(Math.abs(num(waterfall.shareTotal)-100)>.01)blockers.push("SETTLEMENT SHARES MUST TOTAL 100%");if(!waterfall.balanced)blockers.push("SETTLEMENT OUT OF BALANCE");return blockers;}
export default{projectIXIAssetSettlement,calculateIXISettlementWaterfall,getIXISettlementBlockers};