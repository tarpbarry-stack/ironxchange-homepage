import{createIXIAosObjectFinancialDocument,createIXIAosFinancialObjectReference}from"../../../financial-runtime/IXIAosFinancialRuntimeAdapter";
import{runIXIActionNoticeLifecycle}from"../../../../ixi-object-system/IXIActionNoticeEngine";
import{createIXIServiceQuoteDraft,validateIXIServiceQuote}from"./IXIServiceQuoteContract";
const clean=v=>String(v??"").trim();
function push(refs,ref){if(!ref)return;const k=[ref.passportId,ref.externalId,ref.role,ref.label].map(clean).join("|");if(!refs.some(x=>[x.passportId,x.externalId,x.role,x.label].map(clean).join("|")===k))refs.push(ref);}
export async function createIXIServiceQuote({object={},context={},input={},commandId="",idempotencyKey="",metadata={},apiBaseUrl="",headers={},signal}={}){const draft=createIXIServiceQuoteDraft({context,input}),check=validateIXIServiceQuote(draft);if(!check.valid){const e=new Error("Service Quote is incomplete");e.validation=check;throw e;}const cmd=clean(commandId||draft.identity.clientRequestId||`SQ-${Date.now()}`),resolved={...object,passportId:clean(object.passportId||draft.context.primaryPassportId),objectId:clean(object.objectId||draft.context.primaryObjectId),objectType:clean(object.objectType||draft.context.primaryObjectType),label:clean(object.label||draft.context.primaryLabel)},notice=clean(context.primary?.objectId||context.primary?.passportId||resolved.objectId||resolved.passportId);return runIXIActionNoticeLifecycle({objectId:notice,commandId:cmd,source:"ixi-transact-service-quote",savingMessage:"CREATING SERVICE QUOTE...",successMessage:x=>`SERVICE QUOTE ${clean(x?.record?.identity?.number)||"CREATED"}`,errorMessage:"SERVICE QUOTE SAVE FAILED",operation:async()=>{const refs=[];push(refs,createIXIAosFinancialObjectReference({object:context.primary||resolved,role:"asset"}));push(refs,createIXIAosFinancialObjectReference({object:context.entity||{},role:"entity"}));push(refs,createIXIAosFinancialObjectReference({object:context.location||{},role:"location"}));push(refs,createIXIAosFinancialObjectReference({object:context.actor||{},role:"employee"}));if(draft.customer.name)push(refs,{role:"customer",label:draft.customer.name,objectType:"entity",passportId:draft.customer.passportId,externalId:draft.customer.customerId});const response=await createIXIAosObjectFinancialDocument({object:resolved,documentType:"quote",input:{currency:"USD",amount:draft.economics.quotedRevenue,description:`Service Quote · ${draft.customer.name} · ${draft.asset.label}`,status:"draft",financialState:"planned",serviceQuote:draft,references:refs,attachments:draft.documents},additionalReferences:refs,commandId:cmd,idempotencyKey:clean(idempotencyKey||`ixi-service-quote:${cmd}`),metadata:{...metadata,transactModule:"service-quote",quoteSchema:draft.schema,pricingType:draft.commercial.pricingType,revision:draft.identity.revision,customerLabel:draft.customer.name},apiBaseUrl,headers,signal});const id=clean(response?.document?.documentId||response?.financialDocument?.documentId||response?.documentId||cmd),number=clean(response?.document?.documentNumber||response?.financialDocument?.documentNumber)||`SQ-${id.replace(/^SQ-/i,"").slice(-6).toUpperCase()}`,occurredAt=new Date().toISOString();return{record:{...draft,identity:{...draft.identity,serviceQuoteId:id,number},activity:[{eventId:`SQ-CREATE-${Date.now()}`,type:"quote-created",occurredAt,actorId:draft.context.actorId,actorLabel:draft.context.actorLabel}],audit:{...draft.audit,updatedAt:occurredAt}},response};}});}
export default{createIXIServiceQuote};