const clean=v=>String(v??"").trim();
const obj=v=>v&&typeof v==="object"&&!Array.isArray(v)?v:{};
export function createIXIExpenseDraft({context={},workOrder={},input={}}={}){const now=new Date().toISOString(),primary=obj(context.primary),actor=obj(context.actor),location=obj(context.location),wo=obj(workOrder.identity);return {schema:"ixi-expense-v1",identity:{expenseId:"",number:""},context:{entityPassportId:clean(context.entity?.passportId),primaryPassportId:clean(primary.passportId),primaryObjectType:clean(primary.objectType),primaryLabel:clean(primary.label),locationPassportId:clean(location.passportId||context.locationPassportId),locationLabel:clean(location.label||context.locationLabel),workOrderId:clean(wo.workOrderId),workOrderNumber:clean(wo.number),employeePassportId:clean(actor.passportId),employeeLabel:clean(actor.displayName||actor.name||actor.label)},expense:{vendor:clean(input.vendor),description:clean(input.description),amount:Number(input.amount||0),currency:clean(input.currency||"USD").toUpperCase(),category:clean(input.category),expenseDate:clean(input.expenseDate),paymentMethod:clean(input.paymentMethod),referenceNumber:clean(input.referenceNumber),notes:clean(input.notes),receiptRequired:Boolean(input.receiptRequired)},attachments:Array.isArray(input.attachments)?input.attachments:[],status:"draft",createdAt:now,createdBy:clean(actor.passportId||actor.id),revision:1};}
export function validateIXIExpense(expense={}){const e=obj(expense.expense),errors={};if(!clean(e.vendor))errors.vendor="Vendor is required";if(!clean(e.description))errors.description="Description is required";if(!(Number(e.amount)>0))errors.amount="Amount is required";if(!clean(e.expenseDate))errors.expenseDate="Expense date is required";if(e.receiptRequired&&!(expense.attachments||[]).length)errors.receipt="Receipt is required by policy";return {valid:Object.keys(errors).length===0,errors};}
export default {createIXIExpenseDraft,validateIXIExpense};