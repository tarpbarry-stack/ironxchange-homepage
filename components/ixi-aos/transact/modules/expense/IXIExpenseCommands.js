import {createIXIAosExpense,createIXIAosFinancialObjectReference} from "../../../financial-runtime/IXIAosFinancialRuntimeAdapter";
import {createIXIExpenseDraft,validateIXIExpense} from "./IXIExpenseContract";
const clean=v=>String(v??"").trim();
export async function createIXIExpense({object={},context={},workOrder={},input={},commandId="",idempotencyKey="",metadata={},apiBaseUrl="",headers={},signal}={}){const draft=createIXIExpenseDraft({context,workOrder,input}),validation=validateIXIExpense(draft);if(!validation.valid){const error=new Error("Expense is incomplete");error.validation=validation;throw error;}const additionalReferences=[];const locationRef=createIXIAosFinancialObjectReference({object:context.location||{},role:"location"});if(locationRef)additionalReferences.push(locationRef);const employeeRef=createIXIAosFinancialObjectReference({object:context.actor||{},role:"employee"});if(employeeRef)additionalReferences.push(employeeRef);if(clean(draft.context.workOrderId||draft.context.workOrderNumber))additionalReferences.push({passportId:clean(draft.context.workOrderId||draft.context.workOrderNumber),role:"work-order",label:clean(draft.context.workOrderNumber),objectType:"work-order"});const response=await createIXIAosExpense({object,input:{currency:draft.expense.currency,amount:draft.expense.amount,description:draft.expense.description,status:"posted",vendor:draft.expense.vendor,category:draft.expense.category,expenseDate:draft.expense.expenseDate,paymentMethod:draft.expense.paymentMethod,referenceNumber:draft.expense.referenceNumber,notes:draft.expense.notes,attachments:draft.attachments,references:additionalReferences},additionalReferences,commandId,idempotencyKey,metadata:{...metadata,transactModule:"expense",expenseSchema:draft.schema,workOrderId:draft.context.workOrderId,workOrderNumber:draft.context.workOrderNumber},apiBaseUrl,headers,signal});return {draft:{...draft,status:"posted"},response};}
export default {createIXIExpense};