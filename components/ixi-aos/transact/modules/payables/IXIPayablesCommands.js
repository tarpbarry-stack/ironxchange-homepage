import { createIXIAosObjectFinancialDocument, createIXIAosFinancialObjectReference, mergeIXIAosFinancialReferences } from "../../../financial-runtime/IXIAosFinancialRuntimeAdapter";
import { runIXIActionNoticeLifecycle } from "../../../../ixi-object-system/IXIActionNoticeEngine";
const clean=value=>String(value??"").trim();const num=value=>Number.isFinite(Number(value))?Number(value):0;
function refs({context={},payable={},object={}}={}){const list=[];for(const[candidate,role]of[[context.primary||object,"object"],[context.entity||{},"entity"],[context.location||{},"location"],[context.actor||{},"employee"]]){const ref=createIXIAosFinancialObjectReference({object:candidate,role});if(ref)list.push(ref);}if(payable?.vendorLabel)list.push({role:"vendor",label:clean(payable.vendorLabel),objectType:"vendor",passportId:clean(payable.vendorPassportId),externalId:clean(payable.vendorId)});if(payable?.billId)list.push({role:"bill",label:clean(payable.billNumber||payable.billId),objectType:"bill",externalId:clean(payable.billId)});return mergeIXIAosFinancialReferences(list);}
export async function postIXIPayablesPayment({object={},context={},payable={},input={},metadata={},signal}={}){const amount=Math.round(num(input.amount)*100)/100;if(!(amount>0))throw new Error("Payment amount must be greater than zero.");if(amount>num(payable.balance)+0.005)throw new Error("Payment cannot exceed the open A/P balance.");const commandId=clean(input.clientRequestId)||`AP-PAY-${Date.now()}`,references=refs({context,payable,object}),origin=clean(context.primary?.objectId||context.primary?.passportId||object.objectId||object.passportId);return runIXIActionNoticeLifecycle({objectId:origin,commandId,source:"ixi-transact-payables-payment",savingMessage:"POSTING A/P PAYMENT...",successMessage:"A/P PAYMENT POSTED",errorMessage:e=>clean(e?.message)||"A/P PAYMENT FAILED",operation:async()=>{const response=await createIXIAosObjectFinancialDocument({object,documentType:"payment",commandId,idempotencyKey:`ixi-ap-payment:${commandId}`,signal,input:{currency:clean(payable.currency||"USD"),amount,total:amount,subtotal:amount,status:"posted",financialState:"paid",direction:"out",documentDate:clean(input.date)||new Date().toISOString().slice(0,10),paymentMethod:clean(input.method||"ach"),paymentReference:clean(input.reference),description:`A/P payment · ${clean(payable.billNumber)}`,relatedBillId:clean(payable.billId),references},additionalReferences:references,metadata:{...metadata,transactModule:"payables",payablesPayment:true,billDocumentId:clean(payable.billId),billNumber:clean(payable.billNumber)}});return{response,payment:{paymentId:clean(response?.financialDocument?.documentId||response?.document?.documentId||response?.documentId||commandId),amount,date:clean(input.date),method:clean(input.method),reference:clean(input.reference)}};}});}
export async function postIXIPayablesCredit({object={},context={},payable={},input={},metadata={},signal}={}){const amount=Math.round(num(input.amount)*100)/100;if(!(amount>0))throw new Error("Credit amount must be greater than zero.");if(amount>num(payable.balance)+0.005)throw new Error("Credit cannot exceed the open A/P balance.");const commandId=clean(input.clientRequestId)||`AP-CREDIT-${Date.now()}`,references=refs({context,payable,object}),origin=clean(context.primary?.objectId||context.primary?.passportId||object.objectId||object.passportId);return runIXIActionNoticeLifecycle({objectId:origin,commandId,source:"ixi-transact-payables-credit",savingMessage:"POSTING VENDOR CREDIT...",successMessage:"VENDOR CREDIT POSTED",errorMessage:e=>clean(e?.message)||"VENDOR CREDIT FAILED",operation:async()=>{const response=await createIXIAosObjectFinancialDocument({object,documentType:"credit",commandId,idempotencyKey:`ixi-ap-credit:${commandId}`,signal,input:{currency:clean(payable.currency||"USD"),amount,total:amount,subtotal:amount,status:"posted",financialState:"credited",direction:"in",documentDate:clean(input.date)||new Date().toISOString().slice(0,10),description:clean(input.reason)||`Vendor credit · ${clean(payable.billNumber)}`,relatedBillId:clean(payable.billId),references},additionalReferences:references,metadata:{...metadata,transactModule:"payables",vendorCredit:true,billDocumentId:clean(payable.billId),billNumber:clean(payable.billNumber)}});return{response,credit:{creditId:clean(response?.financialDocument?.documentId||response?.document?.documentId||response?.documentId||commandId),amount,date:clean(input.date),reason:clean(input.reason)}};}});}
export default { postIXIPayablesPayment, postIXIPayablesCredit };
