import { useMemo, useState } from "react";
import IXIMachineRail from "../../IXIMachineRail";
import { createIXITransactContext } from "./IXITransactContext";
import { getIXITransactModules } from "./IXITransactModuleRegistry";
import IXIWorkOrderApp from "./modules/work-order/IXIWorkOrderApp";
import IXITechWorkOrderApp from "./modules/tech-work-order/IXITechWorkOrderApp";
import IXINoteApp from "./modules/note/IXINoteApp";
import IXIPhotoApp from "./modules/photo/IXIPhotoApp";
import IXIExpenseApp from "./modules/expense/IXIExpenseApp";
import IXIPurchaseOrderApp from "./modules/purchase-order/IXIPurchaseOrderApp";
import IXIBillStandaloneApp from "./modules/bill/IXIBillStandaloneApp";
import IXITimeStandaloneApp from "./modules/time/IXITimeStandaloneApp";
import IXIMaterialStandaloneApp from "./modules/material/IXIMaterialStandaloneApp";
import IXIAssetAcquisitionApp from "./modules/asset-acquisition/IXIAssetAcquisitionApp";
import IXIRentalExpenseApp from "./modules/rental-expense/IXIRentalExpenseApp";
import IXIRentalIncomeApp from "./modules/rental-income/IXIRentalIncomeApp";
import IXIServiceQuoteApp from "./modules/service-quote/IXIServiceQuoteApp";
import IXITransactStyles from "./IXITransactStyles";
const clean=value=>String(value??"").trim();
function addUnique(values=[],id=""){const key=clean(id);if(!key)return Array.isArray(values)?values:[];return[...new Set([...(Array.isArray(values)?values:[]),key])];}
function appendUniqueRecord(records=[],record={},identityGetter=()=>""){const list=Array.isArray(records)?records:[],identity=clean(identityGetter(record));if(!identity)return[...list,record];const i=list.findIndex(item=>clean(identityGetter(item))===identity);if(i<0)return[...list,record];return list.map((item,index)=>index===i?record:item);}
export default function IXITransactApp({object={},actor={},entity={},activeWorkOrder=null,permissions=[],onClose=null,onOpenModule=null,onSendFront=null,onSendBack=null,onCycleColor=null,onCycleOutline=null,armedDestination="",onSendToArmedDestination=null}){const context=useMemo(()=>createIXITransactContext({object,actor,entity,activeWorkOrder,permissions}),[object,actor,entity,activeWorkOrder,permissions]),modules=useMemo(()=>getIXITransactModules({objectType:context.primary.objectType,permissions:context.permissions}),[context]),[moduleId,setModuleId]=useState(""),[workOrderSnapshot,setWorkOrderSnapshot]=useState(null),active=modules.find(item=>item.id===moduleId)||null,noteOpen=moduleId==="work-order-note",photoOpen=moduleId==="work-order-photo",expenseOpen=moduleId==="expense",purchaseOrderOpen=moduleId==="purchase-order",billOpen=moduleId==="bill",timeOpen=moduleId==="time",materialOpen=moduleId==="material",assetAcquisitionOpen=moduleId==="asset-acquisition",rentalExpenseOpen=moduleId==="rental-expense",rentalIncomeOpen=moduleId==="rental-income",serviceQuoteOpen=moduleId==="service-quote",compactHeader=Boolean(active)||noteOpen||photoOpen||expenseOpen||purchaseOrderOpen||timeOpen||materialOpen||assetAcquisitionOpen||rentalExpenseOpen||rentalIncomeOpen||serviceQuoteOpen,resolvedWorkOrder=workOrderSnapshot||context.activeWorkOrder||null;
async function open(item){if(!item?.id)return;setModuleId(item.id);await onOpenModule?.(item,context,{})}async function workOrderAction(actionId,workOrder,workContext,payload={}){const next=workOrder||resolvedWorkOrder||null;if(next)setWorkOrderSnapshot(next);if(actionId==="note"){setModuleId("work-order-note");return}if(actionId==="photo"){setModuleId("work-order-photo");return}await onOpenModule?.({id:actionId,label:String(actionId||"").toUpperCase(),group:"work-order-action",documentType:actionId},workContext,{workOrder:next,...payload})}
async function saveDirectExpense(expense,input,response){const expenseId=clean(expense?.identity?.expenseId||expense?.identity?.number||expense?.identity?.clientRequestId);if(!expenseId)throw new Error("Saved Expense did not return a stable identity.");const activity={activityId:`ACT-${expenseId}`,type:"expense-recorded",expenseId,amount:Number(expense?.expense?.amount||0),vendor:clean(expense?.expense?.vendor),paymentMethod:clean(expense?.expense?.paymentMethod),reimbursementRequired:Boolean(expense?.reimbursement?.required),occurredAt:new Date().toISOString(),actorLabel:clean(context.actor?.displayName||context.actor?.name||context.actor?.label)};await onOpenModule?.({id:"expense-save",label:"SAVE EXPENSE",group:"spend",documentType:"expense"},context,{expense:{...expense,identity:{...(expense?.identity||{}),expenseId}},input,response,activity,reimbursement:expense?.reimbursement||null,returnTo:"transact-origin"});setModuleId("")}
const changed=(id,label,group,documentType,key)=>(nextRecord,change={},sourceContext=context)=>onOpenModule?.({id:`${id}-${change.action||"change"}`,label,group,documentType},sourceContext,{[key]:nextRecord,change,originatingObject:sourceContext.primary,returnTo:id});
const purchaseOrderChanged=changed("purchase-order","PURCHASE ORDER UPDATE","buy","purchase-order","purchaseOrderRecord"),billChanged=changed("bill","BILL / INVOICE UPDATE","spend","bill","billRecord"),timeChanged=changed("time","TIME UPDATE","work","time-entry","timeRecord"),materialChanged=changed("material","PART / MATERIAL UPDATE","work","material-usage","materialRecord"),assetAcquisitionChanged=changed("asset-acquisition","ASSET ACQUISITION UPDATE","asset","asset-acquisition","assetAcquisition"),rentalExpenseChanged=changed("rental-expense","RENTAL EXPENSE UPDATE","rent","rental","rentalExpense"),rentalIncomeChanged=changed("rental-income","RENTAL INCOME UPDATE","rent","rental","rentalIncome"),serviceQuoteChanged=changed("service-quote","SERVICE QUOTE UPDATE","sell","quote","serviceQuote");
async function techWorkOrderCreated(record,techContext){await onOpenModule?.({id:"tech-work-order-create",label:"CREATE TECH WORK ORDER",group:"work",documentType:"technology-work-order"},techContext,{techWorkOrder:record,returnTo:"tech-work-order"})}async function techWorkOrderChanged(nextRecord,change={},techContext=context){await onOpenModule?.({id:`tech-work-order-${change.action||"change"}`,label:"TECH WORK ORDER UPDATE",group:"work",documentType:"technology-work-order"},techContext,{techWorkOrder:nextRecord,change,returnTo:"tech-work-order"})}
async function saveNote(note,input,response){const noteId=clean(note?.identity?.noteId||note?.identity?.clientRequestId);if(!noteId)throw new Error("Saved Note did not return a stable identity.");const attachment=note?.note?.attachments?.[0]||null,current=resolvedWorkOrder||{},storedNote={...note,identity:{...(note.identity||{}),noteId}},activity={activityId:`ACT-${noteId}`,type:"note-added",noteId,noteType:note?.note?.type||"work-note",title:note?.note?.title||"",body:note?.note?.body||"",occurredAt:note?.audit?.createdAt||new Date().toISOString(),actorLabel:note?.audit?.createdByLabel||clean(context.actor?.displayName||context.actor?.name||context.actor?.label)};let documentProjection=Array.isArray(current.documentProjection)?current.documentProjection:[];if(attachment){const attachmentIdentity=clean(attachment.documentId||attachment.id)||`NOTE-ATTACH:${noteId}:${attachment.fileName||"attachment"}`,document={documentId:attachmentIdentity,title:attachment.fileName||"Note attachment",fileName:attachment.fileName||"",type:attachment.mimeType?.startsWith("image/")?"photo":"other",issuer:note?.audit?.createdByLabel||"",relatedType:"note",relatedId:noteId,relatedLabel:note?.note?.title||noteId,date:note?.audit?.createdAt||new Date().toISOString(),addedBy:note?.audit?.createdByLabel||"",mimeType:attachment.mimeType||"",size:Number(attachment.size||0),persistenceState:attachment.status||"local-pending-upload"};documentProjection=appendUniqueRecord(documentProjection,document,item=>item?.documentId||item?.id)}const next={...current,references:{...(current.references||{}),noteIds:addUnique(current.references?.noteIds,noteId)},notesProjection:appendUniqueRecord(current.notesProjection,storedNote,item=>item?.identity?.noteId||item?.identity?.clientRequestId),activityProjection:appendUniqueRecord(current.activityProjection,activity,item=>item?.activityId),documentProjection};setWorkOrderSnapshot(next);await onOpenModule?.({id:"note-save",label:"SAVE NOTE",group:"work-order-action",documentType:"note"},context,{workOrder:next,note:storedNote,input,response,activity});setModuleId("work-order")}
async function savePhoto(photo,input,response){const photoId=clean(photo?.identity?.photoId||photo?.identity?.clientRequestId);if(!photoId)throw new Error("Saved Photo entry did not return a stable identity.");const current=resolvedWorkOrder||{},media=Array.isArray(photo?.photo?.media)?photo.photo.media:[],attachmentIds=media.reduce((ids,item)=>addUnique(ids,item.mediaId),current.references?.attachmentIds||[]),documents=media.map((item,index)=>({documentId:item.mediaId||`${photoId}-MEDIA-${index+1}`,title:photo?.photo?.title||item.fileName||`Photo ${index+1}`,fileName:item.fileName||"",type:"photo",typeLabel:photo?.photo?.type==="damage"?"DAMAGE":photo?.photo?.type==="before-after"?"BEFORE / AFTER":photo?.photo?.type==="reference"?"REFERENCE":"WORK PHOTO",issuer:photo?.audit?.createdByLabel||"",relatedType:"photo",relatedId:photoId,relatedLabel:photo?.photo?.title||photoId,date:photo?.photo?.occurredAt||photo?.audit?.createdAt||new Date().toISOString(),addedBy:photo?.audit?.createdByLabel||"",mimeType:item.mimeType||"image/jpeg",size:Number(item.size||0),previewUrl:item.previewUrl||"",persistenceState:item.status||"local-pending-upload"}));let documentProjection=Array.isArray(current.documentProjection)?current.documentProjection:[];for(const document of documents)documentProjection=appendUniqueRecord(documentProjection,document,item=>item?.documentId||item?.id);const storedPhoto={...photo,identity:{...(photo.identity||{}),photoId}},activity={activityId:`ACT-${photoId}`,type:"photo-added",photoId,photoType:photo?.photo?.type||"work-photo",title:photo?.photo?.title||"",count:media.length,occurredAt:photo?.photo?.occurredAt||new Date().toISOString(),actorLabel:photo?.audit?.createdByLabel||clean(context.actor?.displayName||context.actor?.name||context.actor?.label)},next={...current,references:{...(current.references||{}),photoIds:addUnique(current.references?.photoIds,photoId),attachmentIds},photoProjection:appendUniqueRecord(current.photoProjection,storedPhoto,item=>item?.identity?.photoId||item?.identity?.clientRequestId),documentProjection,activityProjection:appendUniqueRecord(current.activityProjection,activity,item=>item?.activityId)};setWorkOrderSnapshot(next);await onOpenModule?.({id:"photo-save",label:"SAVE PHOTO",group:"work-order-action",documentType:"photo"},context,{workOrder:next,photo:storedPhoto,input,response,activity,documents});setModuleId("work-order")}
if(billOpen)return <IXIBillStandaloneApp context={context} object={object} authority={actor?.billAuthority||actor?.financialAuthority||actor?.purchasingAuthority||{}} onBack={()=>setModuleId("")} onRecordChange={billChanged}/>;
return <div className={`ixi-transact-app ixi-transact-v13 board-color-none board-outline-1 ${compactHeader?"module-open":"home-open"}`}><header className="tx-header"><div className="tx-brand"><span>IXI TRAN$ACT</span>{!compactHeader?<><strong>{context.primary.label}</strong><small>{context.primary.objectType||"AOS OBJECT"}</small></>:null}</div><button className="tx-close" type="button" onClick={()=>onClose?.()} aria-label="Close TRAN$ACT">×</button></header><main className="tx-body">{noteOpen?<IXINoteApp context={context} workOrder={resolvedWorkOrder||{}} onCancel={()=>setModuleId("work-order")} onSave={saveNote}/>:photoOpen?<IXIPhotoApp context={context} workOrder={resolvedWorkOrder||{}} onCancel={()=>setModuleId("work-order")} onSave={savePhoto}/>:expenseOpen?<IXIExpenseApp context={context} workOrder={resolvedWorkOrder} onCancel={()=>setModuleId("")} onSave={saveDirectExpense}/>:purchaseOrderOpen?<IXIPurchaseOrderApp context={context} onBack={()=>setModuleId("")} onRecordChange={purchaseOrderChanged}/>:timeOpen?<IXITimeStandaloneApp context={context} object={object} onBack={()=>setModuleId("")} onRecordChange={timeChanged}/>:materialOpen?<IXIMaterialStandaloneApp context={context} object={object} onBack={()=>setModuleId("")} onRecordChange={materialChanged}/>:assetAcquisitionOpen?<IXIAssetAcquisitionApp context={context} object={object} relatedTransactions={object?.assetFinancialTransactions||object?.relatedFinancialRecords||object?.financialRecords||[]} onBack={()=>setModuleId("")} onRecordChange={assetAcquisitionChanged}/>:rentalExpenseOpen?<IXIRentalExpenseApp context={context} object={object} relatedTransactions={object?.rentalFinancialTransactions||object?.relatedFinancialRecords||object?.financialRecords||[]} onBack={()=>setModuleId("")} onRecordChange={rentalExpenseChanged}/>:rentalIncomeOpen?<IXIRentalIncomeApp context={context} object={object} relatedTransactions={object?.rentalIncomeTransactions||object?.rentalFinancialTransactions||object?.relatedFinancialRecords||object?.financialRecords||[]} onBack={()=>setModuleId("")} onRecordChange={rentalIncomeChanged}/>:serviceQuoteOpen?<IXIServiceQuoteApp context={context} object={object} onBack={()=>setModuleId("")} onRecordChange={serviceQuoteChanged} onCreateServiceWorkOrder={async quote=>{const draft={identity:{workOrderId:`CSWO-${Date.now()}`,number:`CSWO-${Date.now().toString().slice(-6)}`},schema:"ixi-customer-service-work-order-v1",customer:quote.customer,asset:quote.asset,quote:{serviceQuoteId:quote.identity?.serviceQuoteId,number:quote.identity?.number,revision:quote.identity?.revision,authorizedRevenue:quote.economics?.authorizedRevenue,pricingType:quote.commercial?.pricingType,acceptedOptionIds:quote.acceptance?.acceptedOptionIds},work:{type:"customer-service",title:quote.request?.problem,description:quote.request?.customerScope,status:"open"},context:quote.context,createdAt:new Date().toISOString()};await onOpenModule?.({id:"customer-service-work-order-create",label:"CREATE CUSTOMER SERVICE WORK ORDER",group:"work",documentType:"work-order"},context,{customerServiceWorkOrder:draft,serviceQuote:quote,returnTo:"service-quote"});return draft}}/>:active?.id==="work-order"?<IXIWorkOrderApp context={context} initialWorkOrder={resolvedWorkOrder} onBack={()=>setModuleId("")} onCreate={async(draft,workContext)=>{setWorkOrderSnapshot(draft);await onOpenModule?.({id:"work-order-create",label:"CREATE WORK ORDER",group:"work",documentType:"work-order"},workContext,{workOrder:draft})}} onAction={workOrderAction}/>:active?.id==="technology-work"?<IXITechWorkOrderApp context={context} onBack={()=>setModuleId("")} onCreate={techWorkOrderCreated} onRecordChange={techWorkOrderChanged}/>:active?<div className="tx-module"><button className="tx-back" onClick={()=>setModuleId("")}>‹ TRAN$ACT</button><div className="tx-module-title"><span>{active.group.toUpperCase()}</span><strong>{active.label}</strong></div><div className="tx-module-placeholder"><b>{active.label}</b><span>MODULE CHASSIS READY</span><small>{active.documentType} · {context.primary.label}</small></div></div>:<>{context.activeWorkOrder?<button className="tx-open-work" onClick={()=>{setWorkOrderSnapshot(context.activeWorkOrder);open({id:"work-order",label:"CONTINUE WORK",group:"work",documentType:"